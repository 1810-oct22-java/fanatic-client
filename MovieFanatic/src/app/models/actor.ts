import { SafeStyle } from "@angular/platform-browser";

export class Actor {
    id: string;
    firstName: string;
    lastName: string;
    age: number;
    gender: string;
    birthDate: Date;
    pic_path:string;
}